[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
# tests/metaweave_relationships/ 是手工调试脚本集合（README 已说明"直接运行，
# 不通过 pytest"），依赖真实 output/json 与数据库数据，与 doc 15 改造范围无关，
# 不纳入自动化测试收集。
addopts = --ignore=tests/metaweave_relationships

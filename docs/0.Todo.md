1.丰富和规范化表和字段的数据画像。
2.保持LLM与非LLM得关系，当前LLM的信息有些不够多。
3.完善表之间关联关系的发现逻辑，能更好的把非LLM与LLM结合在一起。
4.基于output中的文件，设计postgresql表结构，将output中的文件内容写入到数据库表中，为开发前端WEBUI的数据资源目录做准备。
5.设计前端WEBUI的界面，包括数据资源目录、数据资源详情、数据资源关系图、数据资源统计分析等。
6.我要检讨和回顾整个项目的缓存机制，它哪些地方使用了缓存，如何清楚缓存。缓存包含了哪些内容？是否需要在本地建立完整的缓存机制。

---
1.完成 json_llm与 json步骤的整合。
2.修改 三个属性的名字：
    我现在要修改字段的 semantic_role:
    audit -> auditing 
    enum -> flag
    然后增加一个新的 semantic_role: complex
3.增加新的属性和计算逻辑：
4.整合 rel_llm与 rel的步骤：
5.整合 cql 和 cql_llm的步骤：
  - 有很多细化的算法要澄清。
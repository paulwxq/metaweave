# rel命令流程图添加说明

**添加时间**: 2025-12-30  
**文档**: `docs/6_rel命令执行流程详解.html`

---

## 🎨 新增功能：完整执行流程图

为了让读者更直观地理解整个关系发现流程，在HTML文档的"命令概述"部分添加了一个**交互式流程图**。

---

## 📍 流程图位置

- **章节**：第1节"命令概述"末尾
- **目录**：在目录中新增"⭐ 完整流程图（新增）"链接
- **锚点**：`#flowchart`，可直接跳转

---

## 🎯 流程图内容

### 流程节点（共9个）

| 序号 | 节点名称 | 类型 | 说明 |
|------|---------|------|------|
| 1 | CLI 入口 | 起始节点 | 解析命令行参数，路由到rel步骤 |
| 2 | Pipeline 初始化 | 起始节点 | 加载配置，初始化各模块 |
| 3 | JSON加载 + 外键直通 | 阶段1 | 加载元数据，提取外键关系 |
| 4 | 候选关系生成 | 阶段2 | 复合键+单列候选 |
| 5 | 候选关系评分 | 阶段3 | 4维度评分+基数推断 |
| 6 | 决策过滤 + 抑制 | 阶段4 | 阈值过滤+抑制规则 |
| 7 | 结果输出 | 阶段5 | JSON+Markdown输出 |
| 8 | 关系发现完成 | 结束节点 | 统计结果 |

### 箭头标注（数据流向）

每个箭头都标注了传递的数据：
- "创建Pipeline"
- "开始发现"
- "外键关系 + 表元数据"
- "候选关系列表"
- "评分后的候选"
- "接受的关系 + 被抑制的关系"
- "完成"

---

## 💎 流程图特点

### 1. **视觉层次分明**

```
起始节点（紫色渐变）
    ↓
阶段节点（白色+绿色边框+阶段标签）
    ↓
结束节点（绿色渐变）
```

### 2. **交互效果**

- ✨ **悬停效果**：鼠标悬停时节点会轻微上浮，阴影加深
- 🎯 **清晰标注**：每个箭头显示数据流向
- 📱 **响应式设计**：自动适配移动端

### 3. **信息完整**

每个节点包含：
- **节点标题**：简洁的功能描述
- **详细说明**：关键操作步骤（多行显示）
- **模块名称**：对应的代码模块（等宽字体）

---

## 🎨 样式设计

### 颜色方案

| 元素 | 颜色 | 用途 |
|------|------|------|
| 起始节点 | 紫色渐变 (#667eea → #764ba2) | CLI入口、Pipeline初始化 |
| 阶段节点 | 白色 + 绿色边框 (#48bb78) | 5个处理阶段 |
| 结束节点 | 绿色渐变 (#48bb78 → #38a169) | 完成状态 |
| 箭头 | 紫色 (#667eea) | 数据流向 |
| 标签 | 橙色背景 (#fffaf0) | 数据说明 |

### 布局特点

- **垂直流式布局**：从上到下清晰展示流程
- **居中对齐**：所有节点居中，易于阅读
- **间距统一**：节点间距15px，箭头标签位置统一
- **最大宽度**：节点最大600px，适配各种屏幕

---

## 📊 技术实现

### 纯CSS实现

```css
/* 流程节点 */
.flow-node {
    background: white;
    border: 3px solid #667eea;
    border-radius: 8px;
    padding: 20px 30px;
    /* 悬停效果 */
    transition: all 0.3s ease;
}

/* 阶段标签（伪元素） */
.flow-node.stage::before {
    content: attr(data-stage);
    position: absolute;
    top: -12px;
    background: #48bb78;
    /* ... */
}

/* 箭头 */
.flow-arrow {
    border-top: 20px solid #667eea;
    /* 使用CSS三角形技巧 */
}

/* 箭头标签（伪元素） */
.flow-arrow::before {
    content: attr(data-label);
    /* ... */
}
```

### 响应式设计

```css
@media (max-width: 768px) {
    /* 移动端优化 */
    .flow-split {
        flex-direction: column;
    }
}
```

---

## 🔍 流程图与文档的对应关系

| 流程图节点 | 对应章节 | 说明 |
|-----------|---------|------|
| CLI 入口 | 第2节 | CLI入口解析 |
| Pipeline 初始化 | 第4节 | 关系发现管道 |
| 阶段1 | 第3节 | JSON加载与外键直通 |
| 阶段2 | 第5节 | 候选关系生成 |
| 阶段3 | 第6节 | 候选关系评分 |
| 阶段4 | 第7节 | 决策过滤与抑制 |
| 阶段5 | 第8节 | 结果输出 |
| 完成 | 第9节 | 总结与关键指标 |

---

## 📝 补充说明框

在流程图下方添加了**数据流向说明框**：

```
🔄 数据流向：
• 输入：output/json/*.json（Step 2生成的表元数据）
• 中间产物：外键关系 → 候选关系 → 评分候选 → 接受关系
• 输出：output/rel/relationships_global.json + relationships_global.md
```

---

## 💡 使用建议

### 快速导航
1. 从目录点击"⭐ 完整流程图（新增）"快速跳转
2. 查看流程图了解整体流程
3. 点击各节点对应的章节深入了解细节

### 学习路径
1. **新手**：先看流程图 → 再读详细章节
2. **熟悉者**：直接跳转到感兴趣的阶段
3. **开发者**：对照流程图 + 代码模块名称定位源码

---

## ✅ 验证检查

- [x] 流程图位置合理（第1节末尾）
- [x] 目录链接正确（可跳转）
- [x] 节点数量正确（9个）
- [x] 箭头标注完整（7个）
- [x] 颜色区分清晰（3种类型）
- [x] 悬停效果正常
- [x] 响应式适配
- [x] 与代码一致（模块名称准确）

---

## 🎉 效果展示

**流程图核心优势**：
1. ✅ **一图胜千言**：5个阶段一目了然
2. ✅ **数据流清晰**：每个箭头都标注了传递的数据
3. ✅ **模块对应准确**：每个节点显示对应的代码模块
4. ✅ **交互友好**：悬停效果提升用户体验
5. ✅ **风格统一**：与整个HTML文档风格一致

---

**添加完成！流程图让复杂的关系发现流程变得清晰易懂** ✨






